---Jetson commands ---
source ~/AgricultureBot/venv/Scripts/activate
uvicorn main:app --host 0.0.0.0 --port 8000

for pip installs do pip3 install import_name



python -m venv venv
venv/Scripts/Activate


pip install pyserial
pip install inputs
pip install fastapi
pip install uvicorn
pip install websockets

#uvicorn file_to_be_ran:app_name -- reload -> runs uvicorn server
uvicorn main:app --reload
uvicorn main:app --host 0.0.0.0 --port 8000
