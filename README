python -m venv venv
venv/Scripts/Activate


pip install pyserial
pip install inputs
pip install fastapi
pip install uvicorn

#uvicorn file_to_be_ran:app_name -- reload -> runs uvicorn server
uvicorn main:app --reload

send signal -> Invoke-RestMethod -Uri "http://127.0.0.1:8000/items?item=apple" -Method POST